/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { OpScreenComponent } from './op-screen.component';

describe('OpScreenComponent', () => {
  let component: OpScreenComponent;
  let fixture: ComponentFixture<OpScreenComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ OpScreenComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(OpScreenComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
